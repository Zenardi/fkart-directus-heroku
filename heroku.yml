build:
  docker:
    web: Dockerfile
  # config:
  #   RAILS_ENV: development
  #   FOO: bar

run:
  # web: bundle exec puma -C config/puma.rb
  # web: node cli.js bootstrap && pm2-runtime start ecosystem.config.cjs;
  web: npx directus start
